@model Forum.UI.Models.Subject

<div class="container">
    <h1 class="text-center">Add subject</h1>
    <div class="row">
        <div class="mx-auto w-50">
            <form id="createSubject" asp-controller="Subjects" asp-action="AddSubject" method="post">
                <div class="form-group">
                    <label asp-for="Title" class="control-label">Title</label>
                    <input asp-for="Title" class="form-control bg-light" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Message" class="control-label">Message</label>
                    <textarea asp-for="Message" class="form-control bg-light" style="height:300px;"></textarea>
                    <span asp-validation-for="Message" class="text-danger"></span>
                </div>
                <br />
                <div class="text-left">
                    <button class="btn btn-primary" type="submit">Add</button>
                </div>

                @if (!ViewData.ModelState.IsValid)
                {
                    if (ViewData.ModelState[string.Empty] != null)
                    {
                        <div class="error-summary">
                            @foreach (var error in ViewData.ModelState[string.Empty].Errors)
                            {
                                <div class="error-item text-danger">
                                    @error.ErrorMessage
                                </div>
                            }
                        </div>
                    }
                }
            </form>
        </div>
    </div>
</div>